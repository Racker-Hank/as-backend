package vnu.uet.AppointmentScheduler.constants;

public enum AssessmentStepStatus {
	IN_QUEUE,
	READY,
	IN_PROGRESS,
	AWAITING_NEXT_STEPS,
	AWAITING_TEST_RESULTS,
	RE_ENTRY,
	COMPLETED,
	RESCHEDULED,
	CANCELLED
}
